

extern int
engine(
  Parameters parameters,
  TFormatIndices* indices,
  Map* targetMap,
  Map* corpusMap,
  Map* targetMaskMap,
  Map* corpusMaskMap
  //, GimpDrawable *  // ANIMATE, but no Gimp types here
  );
